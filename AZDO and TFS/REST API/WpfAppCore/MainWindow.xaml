<Window x:Class="WpfAppCore.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfAppCore"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>

    </Window.Resources>
    <Grid>
        <StackPanel>
            <Label Content="CB1OC {Binding AvailableCollectionsOC)" />
            <ComboBox x:Name="CB1OC"
                ItemsSource="{Binding AvailableCollections}"
                      DisplayMemberPath="Name"
                      SelectedValuePath="Details"
                      SelectedItem="{Binding SelectedCollection, Mode=TwoWay}">
            </ComboBox>
            <TextBox x:Name="TB1OC" Text="{Binding SelectedCollection.Details.Uri }" />
            <TextBox x:Name="TB2OC" Text="{Binding SelectedCollection.Details.PAT }" />

            <Button Click="GetProjects_Click">GetProjects</Button>
            <Button Click="GetProcessList_Click">Get Process List</Button>
            <Button Click="SampleREST_Click">SampleREST</Button>
            <Button Click="PersonalAccessToken_Click">PersonalAccessToken</Button>
            <Button Click="MicrosoftAccount_Click">MicrosoftAccount</Button>
            <Button Click="OAuth_Click">OAuth</Button>
            <Button Click="AzureActiveDirectory_Click">AzureActiveDirectory</Button>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="500" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!--<ScrollViewer MaxHeight="500">
                    <TextBlock Text="{Binding CallResult}" 
                           TextWrapping="Wrap" Width="Auto" />
                </ScrollViewer>-->

                <TextBox Text="{Binding CallResult}" 
                         VerticalAlignment="Top"
                         Grid.Column="0"
                         MaxHeight="500"
                         ScrollViewer.CanContentScroll="True"
                         TextWrapping="Wrap"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"/>

                <DataGrid ItemsSource="{Binding ResponseHeaders}" Grid.Column="1" AutoGenerateColumns="False"
                                                    ScrollViewer.CanContentScroll="True"
                          ScrollViewer.HorizontalScrollBarVisibility="Auto"
                          ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <DataGrid.Columns>
                        <DataGridTemplateColumn Header="Key">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Key}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        
                        <DataGridTemplateColumn Header="Value">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ItemsControl ItemsSource="{Binding Value}" Margin="0,0,5,0" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>                        
                    </DataGrid.Columns>
                </DataGrid>


                <DataGrid ItemsSource="{Binding ResponseHeaders}" Grid.Column="2" AutoGenerateColumns="False" 
                          ScrollViewer.CanContentScroll="True"
                          ScrollViewer.HorizontalScrollBarVisibility="Auto"
                          ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <DataGrid.Columns>
                        <DataGridTemplateColumn Header="Key">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Key}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="Value">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ItemsControl ItemsSource="{Binding Value}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding}" Margin="0,0,5,0" />
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <StackPanel Orientation="Vertical" />
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                    </ItemsControl>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
                
            </Grid>

        </StackPanel>
    </Grid>
</Window>
